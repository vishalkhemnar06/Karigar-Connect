# Semantic matching service deployment variables
# Render also provides PORT, which this service now respects.
PORT=5100
SEMANTIC_SERVICE_HOST=0.0.0.0
SEMANTIC_SERVICE_PORT=5100
SEMANTIC_DATA_DIR=./data
# Use the absolute smallest model for 512MB RAM
SENTENCE_TRANSFORMER_MODEL=paraphrase-MiniLM-L3-v2
